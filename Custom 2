<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>IC.Eskimoz</title>
  <style>
    :root {
      --bg: #f0f0f0;
      --text: #111;
      --card: #fff;
      --accent: #f4c10f;
    }

    [data-theme="dark"] {
      --bg: #121212;
      --text: #f0f0f0;
      --card: #1f1f1f;
      --accent: #f4c10f;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Segoe UI', sans-serif;
      transition: background 0.4s, color 0.4s;
    }

    body {
      background: var(--bg);
      color: var(--text);
    }

    header {
      background: var(--card);
      padding: 1.2rem 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      box-shadow: 0 2px 6px rgba(0,0,0,0.2);
    }

    header h1 {
      font-size: 1.8rem;
    }

    .theme-toggle {
      background: none;
      border: 2px solid var(--accent);
      color: var(--accent);
      padding: 5px 10px;
      border-radius: 5px;
      cursor: pointer;
    }

    .section {
      padding: 3rem 1rem;
      max-width: 1000px;
      margin: auto;
      animation: fadeIn 1s ease;
    }

    .card {
      background: var(--card);
      padding: 2rem;
      border-radius: 10px;
      box-shadow: 0 0 15px rgba(0,0,0,0.1);
      margin-bottom: 2rem;
    }

    footer {
      text-align: center;
      padding: 2rem;
      background: var(--card);
      color: var(--text);
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(30px); }
      to { opacity: 1; transform: translateY(0); }
    }

    @media (max-width: 768px) {
      header {
        flex-direction: column;
        gap: 1rem;
      }
    }
  </style>
</head>
<body>

  <header>
    <h1>🧊 IC.Eskimoz</h1>
    <button class="theme-toggle" onclick="toggleTheme()">Toggle Theme</button>
  </header>

  <section class="section">
    <div class="card">
      <h2>Welcome to IC.Eskimoz</h2>
      <p>This is your dark-mode powered project hub. Style, performance, and originality — all in one.</p>
    </div>

    <div class="card">
      <h2>Features</h2>
      <ul>
        <li>🕶️ Dark/Light mode with memory</li>
        <li>💫 Smooth fade-in animations</li>
        <li>📱 Fully responsive</li>
        <li>🌍 GitHub Pages ready</li>
      </ul>
    </div>
  </section>

  <footer>
    <p>© 2025 IC.Eskimoz. All rights reserved.</p>
  </footer>

  <script>
    function setTheme(theme) {
      document.body.setAttribute('data-theme', theme);
      localStorage.setItem('theme', theme);
    }

    function toggleTheme() {
      const current = document.body.getAttribute('data-theme');
      setTheme(current === 'dark' ? 'light' : 'dark');
    }

    function loadTheme() {
      const saved = localStorage.getItem('theme');
      if (saved) {
        setTheme(saved);
      } else {
        const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
        setTheme(prefersDark ? 'dark' : 'light');
      }
    }

    window.addEventListener('DOMContentLoaded', loadTheme);
  </script>

</body>
</html>
